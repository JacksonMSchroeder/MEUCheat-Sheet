
const DADOMAE = (PARAMETROS) => {  TRATATIVA RETORNO }   if(se) else(nao) while(loop(enquanto)) for(enquanto com inf definidas)

//////////////

// Compara valores                                              IMPORTANTE !!!IMPORTANTE !!! 
==     Igual a
!=     Diferente de
>      Maior que
<     Menor que
>=     Maior ou igual
<=     Menor ou igual

&&     E (As duas coisas tem que ser verdade) 
||     OU (Pelo menos uma tem que ser verdade)
!      NÃO (Inverte: o que era true vira false)

=      Recebe valor
+=     Soma e guarda (x = x + y)
-=     Subtrai e guarda
++     Soma 1 (Incremento)
--     Subtrai 1 (Decremento)
isNaN()   não numero 

////////////

Principais Operadores Aritméticos:                                              IMPORTANTE!!!

Adição (+): Soma dois valores. Também usado para concatenar strings.

Subtração (-): Subtrai o segundo operando do primeiro.

Multiplicação (*): Multiplica dois valores.

Divisão (/): Divide o primeiro operando pelo segundo.

Módulo (%): Retorna o resto da divisão inteira entre dois números.

Exponenciação (**): Eleva o primeiro operando à potência do segundo (ES6).

Incremento (++): Aumenta o valor da variável em 1.

Decremento (--): Diminui o valor da variável em 1. 

////

Fórmula: Math.min(Math.max(VALOR, MIN), MAX)

valor = Math.min(Math.max(valor, 1), 100);

mathmax sempre vai ser o piso, o começom o mathmix sempre o teto o ultimo   IMPORTANTE !!!IMPORTANTE !!!

variavel = Math.max(valor, 1); piso

variavel = Math.min(valor, 100); teto

/////////

MANIPULAR LISTAS                            !!IMPORTANTE, FUI TENTAR ARRAYS MAIS COMPLEXAS E TRAVEI POR N ENTENDER ISSO BEM
const VARIAVEL = ["Espada", "Escudo", "Poção" ];

VARIAVEL.push(`Adaga`); < ADD ITEM NA LISTA
 VARIAVEL[0] = `Espada de Ferro`;
    console.log(VARIAVEL[3]);


////////

// const hoje = new Date();
// if (hoje.getDay() === 3) {                      //lidando com dias para semana               !!IMPORTANTE dias 
//     console.log("Desconto de terça");
// } else {
//     console.log("Hoje não tem desconto");
// }
 

// const hoje = new Date();                                                                                 //horario !IMPORTANTE
// console.log(hoje.getHours(), hoje.getMinutes(), hoje.getSeconds(), hoje.getMilliseconds());  //para lidar com horarios 


/////////////


📥 1. Adicionar e Remover (Gerenciamento de Pilha)              !!IMPORTANTE para dar os comandos.. 

Perfeito para quando um novo jogador entra na lista de suspeitos ou uma carga é despachada.

.push(item): Adiciona ao final da array. (O que você mais usou até agora).

.pop(): Remove o último item.

.unshift(item): Adiciona ao início.

.shift(): Remove o primeiro item.

🔍 2. Localização e Verificação (Busca)

Ideal para saber se um Nick já está na sua lista de banidos.

.indexOf(item): Retorna a posição (índice) do item. Se não achar, retorna -1.

.includes(item): Retorna true ou false (Verdadeiro/Falso) se o item existe na lista.

.length: Não é um método, mas uma propriedade que diz o tamanho total da array.

🛠️ 3. Transformação e Filtro (O Nível "Pro")

Isso aqui vai substituir os seus for gigantes no futuro. É o que chamamos de Métodos Funcionais.

.forEach(): Executa uma função para cada item (como um for, mas mais limpo).

.filter(): Cria uma nova array apenas com os itens que passaram no seu teste (ex: denuncias > 70).

.map(): Transforma todos os itens da array (ex: pegar uma lista de preços e aplicar 10% de desconto em todos).

.find(): Retorna o primeiro item que ele encontrar que bata com a sua busca.

✂️ 4. Corte e Emenda (Manipulação)

.slice(inicio, fim): Tira uma "fatia" da array sem estragar a original.

.splice(posicao, quantosRemover): Remove ou substitui itens em qualquer lugar da array.

.join(" "): Junta todos os itens da array em uma única String (frase), separando pelo que você colocar no parêntese.


////


typeof	Investiga o tipo do dado.                                                       IMPORTANTE !!!


!isNaN():                                                                               IMPORTANTE !!!
 É universal. Qualquer programador no mundo bate o olho e sabe:                    
"Ah, ele está garantindo que isso é um número válido". 


///

 OS INGREDIENTES (Variáveis e Dados)
const: Para tudo que não muda (90% do seu código).

let: Só se você precisar sobrescrever o valor (ex: um contador).

Number(dado): Transforma o texto do input em número real.

 OS UTENSÍLIOS (Funções Arrow)
A estrutura que você vai usar para criar suas ferramentas de cálculo:

JavaScript
const nomeDaFuncao = (param1, param2) => {
    // 1. Processamento
    const resultado = param1 + param2;
    // 2. Entrega
    return resultado;
};
. O ESTOQUE (Arrays - Listas)
Como você queria usar o [m1, m2]:

Criar: const lista = [10, 20, 30];

Acessar: lista[0] (é o 10), lista[1] (é o 20).

Tamanho: lista.length (diz quantos itens tem).

4. O MODO DE PREPARO (Estruturas de Controle)
IF / ELSE (A Trilha)
JavaScript
if (valor > 10) {
    // Faz isso
} else {
    // Faz aquilo
}
SWITCH (O Elevador)
JavaScript
switch (operacao) {
    case '+': return a + b;
    case '-': return a - b;
    default: return "Erro";
}
WHILE / FOR (A Repetição)
while: "Enquanto a condição for real, repita".

JavaScript
let contador = 0;
while (contador < 5) {
    console.log("Repetindo...");
    contador++; // IMPORTANTE: Se não somar, trava o PC!
}
 O GARÇOM (DOM - Interação com HTML)
Como o JS "pega" e "entrega" as coisas na mesa (tela):

Pegar o valor: const x = document.getElementById('meuInput').value;

Mudar o texto: document.getElementById('titulo').innerText = "Novo Texto";

Mudar a cor: document.getElementById('caixa').style.backgroundColor = "blue";

Ouvir o clique: ```javascript
const botao = document.getElementById('btn');
botao.addEventListener('click', () => {
console.log("Alguém clicou!");
});


 O INSPETOR (Testes e Debug)
* **`console.log(variavel)`**: Mostra o valor "escondido" no F12.
* **`console.table(meuArray)`**: Mostra arrays de um jeito bonitão e organizado.
* **`typeof variavel`**: Te diz se o dado é `string` (texto) ou `number`.



---

### 🧩 Como montar seu quebra-cabeça (O Passo a Passo):

Sempre que travar, olhe para esse roteiro:

1.  **ENTRADA:** Use `document.getElementById(...).value` para pegar os dados.
2.  **CONVERSÃO:** Use `Number()` se for fazer conta matemática.
3.  **LÓGICA:** Crie uma `const minhaFuncao = (dados) => { ... }` para processar.
4.  **DECISÃO:** Use `if` ou `switch` se o código precisar escolher um caminho.
5.  **SAÍDA:** Use `.innerText` para mostrar o resultado no HTML ou `console.log` para testar.

**Dica para Joinville:** Quando estiver vendo o Guanabara, tente identificar qual dessas "peças" ele está usando em cada aula. Você vai ver que, no fundo, ele está sempre combinando esses 6 blocos de formas diferentes.

**Quer que eu transforme esse Cheat Sheet em um arquivo .txt ou .md limpo para você salvar no seu PC?**